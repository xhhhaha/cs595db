1. the secret sharing (SSS or ASS) library are in .py. file
2. Notebook implement the specific computation of JOIN, and corresponding.
The environment is Python 3.7. Required module is also included. 
